# Runtime data is owned by the boss's local installation and must never enter Git.
/data/
/app/data/

# The upstream checkout is a local, read-only audit baseline.
/references/v24-source/

# Local secrets and machine-specific configuration.
.env
.env.*
!.env.example
*.local
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3

# Dependencies, build artifacts, logs, and operating-system files.
node_modules/
dist/
build/
coverage/
*.log
.DS_Store

