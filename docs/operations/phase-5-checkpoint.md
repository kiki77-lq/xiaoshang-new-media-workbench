# PHASE 5 验收记录

状态：**PHASE 5 PASS**。独立审查及两项问题的定向复审通过，无剩余审查问题。

基线：`78487307fb946e95936973a2b933f62488fefa0c`。应用版本 `0.1.0`，Schema Version `3`。提交消息：`feat: build analytics and content review`。

## 已实现

- 手动 / CSV / Excel / WorkBuddy-compatible ingestion，保存来源、周期、口径、批次、成功失败行；坏行使用 savepoint 隔离。
- 标准指标快照、13 项指标、4 类序列、独立作品复盘与4类带证据的判断。observed / derived / inferred 分开，计算和推断强制说明与证据。
- 真实数据总览、四平台表现、排行、4类趋势；账号与作品不混加，最新快照去重，缺失不补零，重叠或跨月累计不猜测拆分。
- 内容库进入复盘；平台与周期快照选择；原始指标、图表及证据表、判断与来源信息；刷新仍保留。
- 共享前端 API Client、8页导航与暗色青色视觉保持；SW v5 静态外壳，所有 `/api/` network-only。

## 新鲜验证

- Node：124/124 PASS，包含 PHASE5 后端专项22项、前端指标渲染8项和历史回归。
- 最终全量 Chromium E2E：14/14 PASS（48.5秒）；包含切月失败、混合证据与图例颜色一致性。此前新增专项4/4 PASS（3.3秒）。
- 1440×900 与390×844：8页导航、横向溢出检测、console error=0；新增CSV/Excel→overview→内容→单作品复盘→刷新链路与手动录入通过。
- 临时真实服务：health/meta HTTP200，数据库正常，schema3；源库SHA保持 `d8f8e5b2d10c193d0ea0bf3581e41cc34490e55b`。
- 语法检查、git diff检查通过。生产依赖审计0个已知漏洞；这不等于安全风险为零。
- 截图：`artifacts/phase-5/` 八张，桌面/手机各导入、概览、复盘、混合证据。控制器实际查看图片。

## 独立审查修复

- 新月份请求失败时不得套用旧月份KPI：校验响应周期，旧月份数据隐藏并明确标注其月份。
- 推断点不得混入连续实测曲线：持续显示证据图例，实测绿色、计算青色虚线、推断紫色空心且不与其他点连线。推断位置和值在折叠表外直接显示，包含无障碍说明。
- 两项均先写出失败测试再修复。503故障注入只在专用测试关闭SW以便拦截；正常PWA测试保持SW启用。注入的503会产生预期网络错误，正常流程无console error。

## 数据安全与限制

- 测试均为虚构 FUV 类型结构，使用操作系统临时数据库。既有真实PDF只参考结构，没有复制实际数字；真实 `data/` 条目仍为0。
- `001` / `002` 未修改，新增 `003_metrics_reviews.sql`；0/1/2升级到3、重复迁移和失败前备份有回归覆盖。
- data/artifacts/SQLite/sidecars/secrets均被Git忽略，受保护目录无跟踪文件。
- 导入最多1 MiB、1000行、64列；Excel首表，公式/错误单元格拒绝，不支持加密或ZIP64。压缩工作簿有16MiB实际解压上限、目录和本地分配提示验证。
- 同对象同周期的新快照整体替代旧快照用于展示；缺失指标不会从旧快照偷偷补齐。原历史仍保留在数据库中。
- official_api仅保留适配位置，没有调用外部AI/平台接口；WorkBuddy Skill尚属PHASE7。
- Node SQLite experimental 与测试终端颜色变量提示为已知运行器警告，不是浏览器 console error。
